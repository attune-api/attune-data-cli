dependencies:
  override:
    - ./gradlew dependencies

test:
  override:
    - ./gradlew build

artifacts:
  branch: master
  commands:
    - ./gradlew distZip
    - cp build/distributions $CIRCLE_ARTIFACTS